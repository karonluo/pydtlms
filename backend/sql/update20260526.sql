-- 2026-05-26 数据库更新脚本
--
-- 当前状态：今日无需要新增执行的数据库结构或数据增量。
--
-- 说明：
-- 1. 2026-05-26 当天完成的改动主要集中在：
--    - 管理端报名信息查看抽屉的展示顺序调整；
--    - 注册学生按报名号查询；
--    - Dashboard 新增“本科院校前二十”统计卡片与学生清单弹框；
--    - 相关后端查询与接口补充。
-- 2. 上述改动均复用现有正式表字段与关系，不涉及新增表、列、索引，也不需要额外数据回填。
-- 3. 当前工作区中与数据库相关的非破坏性升级内容，已由以下脚本覆盖：
--    - backend/sql/update20260521.sql
--      用于新增 dtlms_background_assessments 表，以及放宽
--      dtlms_portal_application_preferences.research_center_name 的非空约束。
-- 4. 因此，2026-05-26 无需重复追加执行新的 DDL / DML，避免重复维护。
--
-- 若今日后续再出现新的数据库结构或数据变更，请继续追加在本文件下方，
-- 并保持脚本默认无破坏性、可重复执行、可审计。

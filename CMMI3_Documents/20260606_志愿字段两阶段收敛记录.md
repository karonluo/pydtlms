# 2026-06-06 志愿字段两阶段收敛记录

## 背景

- 目标：让 Dashboard 志愿统计与“注册学生导出”口径一致。
- 原因：历史上存在研究中心相关字段、回退字段与多来源取值，导致同一筛选条件下统计不一致。

## 第一阶段（今日上线）

- 范围：仅做无破坏性改动，不删除数据库字段。
- 已执行内容：
  1. Dashboard 第一志愿统计去掉回退链路，仅按 `dtlms_portal_application_preferences.preference_order=1` 的导师名统计。
  2. 注册学生导出模板移除研究中心相关列与研究中心导师ID相关列。
  3. 导出动态志愿列仅保留“志愿N导师”。

## 第二阶段（上线后执行）

- 目标：删除数据库中不再使用的研究中心相关字段，彻底收敛为“第一志愿导师/第二志愿导师”。
- 待办清单：
  1. 梳理并确认可删除字段：
     - `dtlms_portal_students.selected_team_id`
     - `dtlms_portal_students.selected_team_name`
     - `dtlms_recruitment_applications.first_choice_team_id`
     - `dtlms_recruitment_applications.second_choice_team_id`
     - `dtlms_portal_application_preferences.team_id`
     - `dtlms_portal_application_preferences.research_center_name`
     - 以及所有相关索引、外键与回填 SQL。
  2. 新增当日无破坏补丁脚本，先停写旧字段，再做灰度验证。
  3. 完成后再提交真正删列脚本（分环境执行，先测试库后生产）。
  4. 同步修订后端 schema、查询、导出、单测与上线文档。

## 上线后验证

- 验证项：
  1. Dashboard 第一/第二志愿导师统计与导出同口径。
  2. 用同一份“注册学生全量导出”按导师筛选后，数量与 Dashboard 一致。
  3. 导出文件不再出现研究中心与研究中心导师ID列。

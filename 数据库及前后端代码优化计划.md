# 数据库及前后端代码优化计划

## 目标
- 清理历史 JSONB / runtime 方案。
- 用范式表替代主业务数据的半结构化存储。
- 保持报名号 candidate_no 一旦生成不可变。
- 保持数据库更新、后端代码、前端页面、测试用例同步。

## 总体原则
- 先执行数据库变更，再写入当天的 `updateYYYYMMDD.sql`。
- 每批只改一个主题，改完就测。
- 读取链优先走范式表，JSONB 仅允许短期兼容读，不再新增写入。
- 代码、脚本、文档、测试必须一起更新。

## 迭代安排

### 第 1 批：主业务表去 JSONB
- 目标：把招生、注册学生、培养、学位等主表中的 JSONB 草稿/快照字段拆出去。
- 做法：新增或补齐结构化子表，迁移历史数据，删除旧 JSONB 列。
- 产物：初始化脚本、当天 update 脚本、同步写路径、导出逻辑、单测。

### 第 2 批：流程和运行态去 JSONB
- 目标：处理 workflow、runtime、历史日志里的 payload / json_value_ / snapshot。
- 做法：先补范式表，再改读写接口，最后移除旧 JSONB 写入。
- 产物：流程兼容层调整、状态同步调整、回归测试。

### 第 3 批：兼容层收口
- 目标：清理旧迁移脚本和历史兼容代码，避免以后误回写 JSONB。
- 做法：把旧脚本改成废弃说明或只保留只读兼容，不再新增字段。
- 产物：废弃迁移脚本、代码注释收口、文档更新。

### 第 4 批：前后端收尾
- 目标：把页面、接口、导出、筛选、详情页全部对齐到范式表。
- 做法：删掉依赖 JSONB 的旧字段展示和旧提交逻辑，统一使用标准接口。
- 产物：前端页面、API 入参出参、导出模板、测试用例。

## 每批执行顺序
1. 先改数据库结构。
2. 再执行实际数据库变更。
3. 再写入当天 `updateYYYYMMDD.sql`。
4. 再改后端同步、查询、导出代码。
5. 再改前端页面和表单校验。
6. 最后跑单测或必要的回归测试。

## 验收标准
- 不再新增 JSONB 写入路径。
- 主业务数据都能从范式表直接读到。
- candidate_no 不可二次修改。
- 对应批次的测试全部通过。
- 当天更新脚本与实际数据库状态一致。

## 当前优先级
- 先完成主业务表 JSONB 拆分。
- 再处理 workflow / runtime。
- 最后做旧脚本和前后端收尾。